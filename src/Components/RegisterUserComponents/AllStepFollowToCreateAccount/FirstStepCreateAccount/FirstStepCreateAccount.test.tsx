import { render } from '@testing-library/react';
import FirstStepCreateAccount from './FirstStepCreateAccount';

describe('FirstStepCreateAccount', () => {
  test('should render itens', () => {
    // const fn = jest.fn();

    render(<FirstStepCreateAccount />);
  });
});
